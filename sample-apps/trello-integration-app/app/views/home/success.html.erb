<div class="container">
  <% if @error.present? %>
    <h3><%= @error.message %></h3>
  <% else %>
    <div class="row">
      <div class="column">
        <h4>Extension card with id=<%= @extension_card.card_id %> is ready to use. Now you can test trello integration via HubSpot interface deal page.</h4>
      </div>
    </div>
  <% end %>
</div>
